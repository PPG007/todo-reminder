syntax = "proto3";

option go_package = "todo-reminder/proto";

message String {
  string value = 1;
}

message Image {
  bytes data = 1;
}

service ChatGPTService {
  rpc GetTextResponse(String) returns (String);
  rpc GetImageResponse(String) returns (Image);
}
